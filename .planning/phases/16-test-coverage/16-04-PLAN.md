---
phase: 16-test-coverage
plan: "04"
type: execute
wave: 1
depends_on: ["16-03"]
files_modified:
  - .github/workflows/test.yml
  - platform/e2e/screenshots/portal-dashboard.png
  - platform/e2e/screenshots/task-list.png
  - platform/e2e/screenshots/settings.png
  - platform/e2e/screenshots/admin-overview.png
  - platform/e2e/screenshots/admin-billing.png
  - platform/e2e/screenshots/admin-users.png
autonomous: false
gap_closure: true
requirements: [TEST-10, TEST-11]

must_haves:
  truths:
    - "6 baseline PNG screenshots exist in platform/e2e/screenshots/ and are committed to the repository"
    - "CI visual-regression job passes green without --update-snapshots (baselines already present)"
    - "A deliberate CSS change causes the visual regression test to fail with a diff image"
  artifacts:
    - path: "platform/e2e/screenshots/portal-dashboard.png"
      provides: "Portal dashboard visual baseline"
    - path: "platform/e2e/screenshots/task-list.png"
      provides: "Task list visual baseline"
    - path: "platform/e2e/screenshots/settings.png"
      provides: "Settings visual baseline"
    - path: "platform/e2e/screenshots/admin-overview.png"
      provides: "Admin overview visual baseline"
    - path: "platform/e2e/screenshots/admin-billing.png"
      provides: "Admin billing visual baseline"
    - path: "platform/e2e/screenshots/admin-users.png"
      provides: "Admin users visual baseline"
    - path: ".github/workflows/test.yml"
      provides: "CI workflow with standard --project=visual (no --update-snapshots)"
      contains: "npx playwright test --project=visual"
  key_links:
    - from: "platform/e2e/visual-regression.spec.ts"
      to: "platform/e2e/screenshots/"
      via: "toHaveScreenshot baseline PNG files"
      pattern: "toHaveScreenshot.*\\.png"
    - from: ".github/workflows/test.yml"
      to: "platform/e2e/screenshots/"
      via: "npx playwright test --project=visual reads committed baselines"
      pattern: "npx playwright test --project=visual"
---

<objective>
Close the single verification gap from Phase 16: generate and commit the 6 Playwright visual regression baseline PNG screenshots so the CI visual-regression job can pass.

Purpose: The visual regression spec and CI workflow are correctly implemented, but zero baseline PNGs exist in the repository. The first CI run of `npx playwright test --project=visual` will fail with "snapshot does not exist" errors because `--update-snapshots` is not used and no baselines are committed. This plan generates Linux-canonical baselines via CI and commits them.

Output: 6 PNG files in `platform/e2e/screenshots/`, CI workflow restored to standard mode (no `--update-snapshots`), both CI jobs passing green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-test-coverage/16-03-SUMMARY.md
@.planning/phases/16-test-coverage/16-VERIFICATION.md

<interfaces>
From platform/e2e/visual-regression.spec.ts:
- 6 toHaveScreenshot calls with names: portal-dashboard.png, task-list.png, settings.png, admin-overview.png, admin-billing.png, admin-users.png
- All use maxDiffPixelRatio: 0.01 and animations: 'disabled'

From platform/playwright.config.ts:
- snapshotPathTemplate: "e2e/screenshots/{arg}{ext}" — baselines go to platform/e2e/screenshots/{name}.png
- visual project depends on setup project (auth storageState)

From .github/workflows/test.yml:
- visual-regression job: `npx playwright test --project=visual` (line 44)
- Uses secrets: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, E2E_EMAIL, E2E_PASSWORD
- Uploads playwright-report/ as artifact on failure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify CI workflow to generate baselines and upload as artifact</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml
    - platform/e2e/visual-regression.spec.ts
    - platform/playwright.config.ts
  </read_first>
  <action>
Modify `.github/workflows/test.yml` to temporarily generate baselines on the first CI run:

1. In the `visual-regression` job, change the Playwright run command from:
   `npx playwright test --project=visual`
   to:
   `npx playwright test --project=visual --update-snapshots`

2. Add a new step AFTER the Playwright run step (and BEFORE the existing failure artifact upload) to ALWAYS upload the generated screenshots as an artifact:

```yaml
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: visual-baselines
          path: platform/e2e/screenshots/
          retention-days: 7
```

This ensures that even if some tests fail for other reasons, the generated baseline PNGs are available for download.

Do NOT change anything else in the workflow file. The `integration` job and all other steps remain unchanged.
  </action>
  <verify>
    <automated>grep -n "update-snapshots" /Users/jw.castillo/Desktop/vantix-kit/.github/workflows/test.yml && grep -n "visual-baselines" /Users/jw.castillo/Desktop/vantix-kit/.github/workflows/test.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/test.yml contains `npx playwright test --project=visual --update-snapshots`
    - .github/workflows/test.yml contains `name: visual-baselines` in an upload-artifact step
    - .github/workflows/test.yml contains `path: platform/e2e/screenshots/` in the visual-baselines upload step
    - .github/workflows/test.yml contains `if: always()` on the baselines upload step
    - The `integration` job is unchanged (still runs `npm run test:run`)
    - The existing failure artifact upload (`visual-regression-diffs`) is still present
  </acceptance_criteria>
  <done>CI workflow is ready to generate and upload baseline PNGs on next push/PR to main</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Trigger CI, download baselines, commit PNGs, revert workflow</name>
  <files>
    - platform/e2e/screenshots/portal-dashboard.png
    - platform/e2e/screenshots/task-list.png
    - platform/e2e/screenshots/settings.png
    - platform/e2e/screenshots/admin-overview.png
    - platform/e2e/screenshots/admin-billing.png
    - platform/e2e/screenshots/admin-users.png
    - .github/workflows/test.yml
  </files>
  <read_first>
    - .github/workflows/test.yml
  </read_first>
  <action>
Human must perform these steps because generating baselines requires: (1) GitHub Actions secrets for Supabase auth, (2) a real CI run on Linux for canonical font rendering, and (3) downloading artifacts from GitHub Actions UI.

Steps:
1. Push the modified test.yml to a branch and open a PR (or push to main) to trigger the CI workflow
2. Wait for the `visual-regression` job to complete in GitHub Actions
3. Download the `visual-baselines` artifact ZIP from the workflow run page
4. Extract PNGs into the repo:
   ```bash
   mkdir -p platform/e2e/screenshots
   unzip ~/Downloads/visual-baselines.zip -d platform/e2e/screenshots/
   ```
5. Verify 6 PNG files exist:
   ```bash
   ls -la platform/e2e/screenshots/*.png
   ```
   Expected files: portal-dashboard.png, task-list.png, settings.png, admin-overview.png, admin-billing.png, admin-users.png
6. Revert test.yml to standard mode:
   - Change `npx playwright test --project=visual --update-snapshots` back to `npx playwright test --project=visual`
   - Remove the `visual-baselines` upload-artifact step entirely (keep only the `visual-regression-diffs` failure step)
7. Commit everything:
   ```bash
   git add platform/e2e/screenshots/*.png .github/workflows/test.yml
   git commit -m "test(16): add visual regression baselines from CI Linux environment"
   ```
8. Push and confirm both CI jobs pass green
  </action>
  <verify>
    <automated>ls /Users/jw.castillo/Desktop/vantix-kit/platform/e2e/screenshots/*.png 2>/dev/null | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - platform/e2e/screenshots/portal-dashboard.png exists and is a valid PNG (file size > 1KB)
    - platform/e2e/screenshots/task-list.png exists and is a valid PNG
    - platform/e2e/screenshots/settings.png exists and is a valid PNG
    - platform/e2e/screenshots/admin-overview.png exists and is a valid PNG
    - platform/e2e/screenshots/admin-billing.png exists and is a valid PNG
    - platform/e2e/screenshots/admin-users.png exists and is a valid PNG
    - .github/workflows/test.yml contains `npx playwright test --project=visual` without --update-snapshots
    - .github/workflows/test.yml does NOT contain `visual-baselines` artifact step
    - Both CI jobs (integration and visual-regression) pass green
  </acceptance_criteria>
  <done>6 Linux-canonical baseline PNGs committed to platform/e2e/screenshots/, CI workflow reverted to standard comparison mode, both CI jobs pass green</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Confirm 6 PNG files exist in `platform/e2e/screenshots/`:
   ```bash
   ls platform/e2e/screenshots/*.png | wc -l
   # Expected: 6
   ```

2. Confirm CI workflow is in standard mode:
   ```bash
   grep "update-snapshots" .github/workflows/test.yml
   # Expected: no output (flag removed)
   ```

3. Confirm CI passes green — both `integration` and `visual-regression` jobs succeed on the latest push
</verification>

<success_criteria>
- 6 baseline PNG files (portal-dashboard.png, task-list.png, settings.png, admin-overview.png, admin-billing.png, admin-users.png) are committed to platform/e2e/screenshots/
- CI visual-regression job passes without --update-snapshots (baselines are compared, not generated)
- CI integration job continues to pass (no regression)
- The verification gap from 16-VERIFICATION.md is closed: "Playwright visual project runs against 6 authenticated pages and captures baseline screenshots" is now TRUE
</success_criteria>

<output>
After completion, create `.planning/phases/16-test-coverage/16-04-SUMMARY.md`
</output>
