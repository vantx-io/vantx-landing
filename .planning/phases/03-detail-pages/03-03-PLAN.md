---
phase: 03-detail-pages
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - 06-landing-pages/about/mission.html
autonomous: false
requirements:
  - PAGE-03

must_haves:
  truths:
    - "about/mission.html loads with dark theme, shared nav, shared footer"
    - "Language toggle switches all text on the mission page"
    - "Mission, Vision, Purpose blocks display with distinct visual styles"
    - "Six value cards display in a 2-column grid"
    - "Eight manifesto items display with dash markers"
    - "Closing CTA section has a working Calendly popup trigger"
  artifacts:
    - path: "06-landing-pages/about/mission.html"
      provides: "Mission, Vision, Values page with header, mission/vision/purpose blocks, values grid, manifesto, and CTA"
      min_lines: 150
  key_links:
    - from: "06-landing-pages/about/mission.html"
      to: "06-landing-pages/i18n/en.json"
      via: "data-i18n attributes referencing mission_page.* keys"
      pattern: "data-i18n=\"mission_page\\."
    - from: "06-landing-pages/about/mission.html"
      to: "06-landing-pages/js/partials.js"
      via: "data-partial nav and footer divs + VANTIX_BASE = '..'"
      pattern: "data-partial=\"nav\""
    - from: "06-landing-pages/about/mission.html"
      to: "06-landing-pages/css/detail.css"
      via: "CSS link in head"
      pattern: "detail\\.css"
---

<objective>
Create the Mission, Vision & Values page: `about/mission.html` (PAGE-03).

Purpose: This page communicates Vantix's identity and values to prospective clients evaluating the company. It uses a different layout than the service pages (content blocks instead of deliverables/timeline) but shares the same dark theme, nav, footer, and i18n infrastructure. Ends with a Calendly CTA for conversion consistency.

Output: Complete HTML file at `06-landing-pages/about/mission.html` with mission/vision/purpose blocks, 6 value cards, 8 manifesto items, and closing CTA.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detail-pages/03-RESEARCH.md
@.planning/phases/03-detail-pages/03-UI-SPEC.md
@.planning/phases/03-detail-pages/03-01-SUMMARY.md
@06-landing-pages/index.html
@06-landing-pages/css/detail.css
@06-landing-pages/i18n/en.json
@06-landing-pages/i18n/es.json
@06-landing-pages/vantix-mision-vision-valores.html

<interfaces>
<!-- CSS classes available from detail.css (created in Plan 01) for mission page -->
- .mission-block, .mission-block__label, .mission-block__heading, .mission-block__body
- .vision-block, .vision-block__label, .vision-block__heading, .vision-block__body
- .purpose-block, .purpose-block__label, .purpose-block__heading
- .values-grid, .value-card, .value-card__num, .value-card__title, .value-card__body
- .manifesto, .manifesto__label, .manifesto__list, .manifesto__item

<!-- From base.css -->
- .section, .container, .section-title, .section-sub, .pill
- .btn, .btn--primary, .btn--lg, .js-calendly-trigger
- .cta-section, .cta-section__inner, .cta-section__heading, .cta-section__sub, .cta-section__actions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create about/mission.html (PAGE-03)</name>
  <files>06-landing-pages/about/mission.html</files>
  <read_first>
    06-landing-pages/index.html
    06-landing-pages/css/detail.css
    06-landing-pages/i18n/en.json
    06-landing-pages/vantix-mision-vision-valores.html
    .planning/phases/03-detail-pages/03-UI-SPEC.md
  </read_first>
  <action>
Create `06-landing-pages/about/mission.html`. This page follows the same HTML shell as the service pages but has a unique content structure: header, mission/vision/purpose blocks, values grid, manifesto, and CTA.

**HEAD section** (identical pattern to service pages, different title/description):
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" data-i18n-attr="content:mission_page.meta_description" content="Vantix mission, vision, and values — data-driven performance engineering with async-first communication and no vendor lock-in.">
  <title data-i18n="mission_page.meta_title">Mission & Values — Vantix</title>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">

  <!-- Design system -->
  <link rel="stylesheet" href="../css/tokens.css">
  <link rel="stylesheet" href="../css/base.css">
  <link rel="stylesheet" href="../css/detail.css">

  <!-- Calendly widget CSS -->
  <link href="https://assets.calendly.com/assets/external/widget.css" rel="stylesheet">

  <!-- Google Analytics 4 — REPLACE G-XXXXXXXXXX with your real Measurement ID before launch -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag() { dataLayer.push(arguments); }
    gtag('js', new Date());
    gtag('config', 'G-XXXXXXXXXX');
  </script>
</head>
```

**BODY structure:**
```html
<body>
  <div data-partial="nav"></div>

  <main id="main-content" tabindex="-1">
    [sections below]
  </main>

  <div data-partial="footer"></div>

  <script>window.VANTIX_BASE = '..';</script>
  <script src="../js/i18n.js"></script>
  <script src="../js/partials.js"></script>

  <script src="https://assets.calendly.com/assets/external/widget.js" type="text/javascript" async></script>

  <script>
    var CALENDLY_URL = 'https://calendly.com/vantix/30min';
    document.addEventListener('click', function (e) {
      var trigger = e.target.closest('.js-calendly-trigger, [href="#calendly"]');
      if (!trigger) return;
      e.preventDefault();
      if (window.Calendly) {
        Calendly.initPopupWidget({ url: CALENDLY_URL });
      } else {
        window.open(CALENDLY_URL, '_blank', 'noopener,noreferrer');
      }
    });
    window.addEventListener('message', function (e) {
      if (e.data && e.data.event === 'calendly.event_scheduled') {
        if (typeof gtag === 'function') {
          gtag('event', 'calendly_booking', {
            event_category: 'engagement',
            event_label: 'demo_scheduled'
          });
        }
      }
    });
  </script>
</body>
</html>
```

**MAIN sections** (inside `<main>`):

**Section 1: Page Header** — Use `.page-hero.section` (no service modifier — uses default blue accent).
- No dot-grid or glow needed for mission page (simpler header). Use `.page-hero` with the background/padding but the visual effects are inherited from CSS.
- Inner div: `.page-hero__inner` centered (`text-align: center; margin: 0 auto;`)
- Pill badge: `<div class="pill" data-i18n="mission_page.header.sub">Performance Engineering . Observability . Fractional SRE</div>`
- Headline: `<h1 class="page-hero__headline" data-i18n="mission_page.mission.heading">...</h1>` (use the mission heading as the page h1 for SEO — this is the page's primary message)
- No subheadline, no CTA buttons, no price in the hero for the mission page (per UI-SPEC: "Placement on mission page: 1 CTA in closing `.cta-section` only (no hero CTA)")

**Section 2: Mission Block** — `<section class="section" id="mission">`
- `.container` wrapper
- `.mission-block` div:
```html
<div class="mission-block">
  <div class="mission-block__label" data-i18n="mission_page.mission.label">Mission</div>
  <h2 class="mission-block__heading" data-i18n="mission_page.mission.heading">...</h2>
  <p class="mission-block__body" data-i18n="mission_page.mission.body">...</p>
</div>
```

**Section 3: Vision Block** — immediately after mission block (same section, no separate section wrapper):
```html
<div class="vision-block">
  <div class="vision-block__label" data-i18n="mission_page.vision.label">Vision</div>
  <h2 class="vision-block__heading" data-i18n="mission_page.vision.heading">...</h2>
  <p class="vision-block__body" data-i18n="mission_page.vision.body">...</p>
</div>
```

**Section 4: Purpose Block** — immediately after vision block (same section):
```html
<div class="purpose-block">
  <div class="purpose-block__label" data-i18n="mission_page.purpose.label">Purpose</div>
  <h2 class="purpose-block__heading" data-i18n="mission_page.purpose.heading">...</h2>
</div>
```

**Section 5: Values** — `<section class="section" id="values" aria-labelledby="values-heading">`
- Section label: `<div class="mission-block__label" data-i18n="mission_page.values.section_label" style="margin-bottom: var(--space-5);">Values</div>` (reuse the label class with accent color)
- `.values-grid` with 6 `.value-card` elements:
```html
<div class="values-grid">
  <div class="value-card">
    <div class="value-card__num" data-i18n="mission_page.values.items.0.num">01</div>
    <h3 class="value-card__title" data-i18n="mission_page.values.items.0.title">Data, not opinions</h3>
    <p class="value-card__body" data-i18n="mission_page.values.items.0.body">...</p>
  </div>
  <!-- items 1-5 -->
</div>
```

**Section 6: Manifesto** — inside a `<section class="section" id="manifesto">`:
```html
<div class="manifesto">
  <div class="manifesto__label" data-i18n="mission_page.manifesto.label">What we believe</div>
  <ul class="manifesto__list">
    <li class="manifesto__item" data-i18n="mission_page.manifesto.items.0">...</li>
    <li class="manifesto__item" data-i18n="mission_page.manifesto.items.1">...</li>
    <!-- items 2-7 (8 total) -->
  </ul>
</div>
```

**Section 7: CTA** — Identical to the service pages. Use `cta_section.*` keys:
```html
<section class="cta-section section" aria-labelledby="page-cta-heading">
  <div class="container">
    <div class="cta-section__inner">
      <h2 id="page-cta-heading" class="cta-section__heading" data-i18n="cta_section.heading">Ready to stop guessing?</h2>
      <p class="cta-section__sub" data-i18n="cta_section.subheading">...</p>
      <div class="cta-section__actions">
        <button class="btn btn--primary btn--lg js-calendly-trigger" type="button" data-i18n="cta_section.cta_primary">Book a Free Demo</button>
      </div>
    </div>
  </div>
</section>
```

Every text-containing element MUST have a `data-i18n` attribute. NO hard-coded text. English fallback text should match `en.json` values.
  </action>
  <verify>
    <automated>test -f 06-landing-pages/about/mission.html && grep -c "data-i18n" 06-landing-pages/about/mission.html | awk '{if ($1 >= 25) print "PASS: " $1 " i18n attributes"; else print "FAIL: only " $1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f 06-landing-pages/about/mission.html` exits 0
    - `grep 'data-partial="nav"' 06-landing-pages/about/mission.html` exits 0
    - `grep 'data-partial="footer"' 06-landing-pages/about/mission.html` exits 0
    - `grep "VANTIX_BASE = '\\.\\.'" 06-landing-pages/about/mission.html` exits 0
    - `grep "js-calendly-trigger" 06-landing-pages/about/mission.html` exits 0
    - `grep "mission-block" 06-landing-pages/about/mission.html` exits 0
    - `grep "vision-block" 06-landing-pages/about/mission.html` exits 0
    - `grep "purpose-block" 06-landing-pages/about/mission.html` exits 0
    - `grep "values-grid" 06-landing-pages/about/mission.html` exits 0
    - `grep "value-card" 06-landing-pages/about/mission.html` exits 0
    - `grep "manifesto__item" 06-landing-pages/about/mission.html` exits 0
    - `grep "mission_page\\.mission\\.label" 06-landing-pages/about/mission.html` exits 0
    - `grep "mission_page\\.vision\\.label" 06-landing-pages/about/mission.html` exits 0
    - `grep "mission_page\\.purpose\\.label" 06-landing-pages/about/mission.html` exits 0
    - `grep "mission_page\\.values\\.items" 06-landing-pages/about/mission.html` exits 0
    - `grep "mission_page\\.manifesto\\.items" 06-landing-pages/about/mission.html` exits 0
    - `grep "cta_section\\.heading" 06-landing-pages/about/mission.html` exits 0
    - `grep "cta_section\\.cta_primary" 06-landing-pages/about/mission.html` exits 0
    - `grep "\\.\\./css/tokens\\.css" 06-landing-pages/about/mission.html` exits 0
    - `grep "\\.\\./css/detail\\.css" 06-landing-pages/about/mission.html` exits 0
    - `grep "\\.\\./js/i18n\\.js" 06-landing-pages/about/mission.html` exits 0
    - `grep "\\.\\./js/partials\\.js" 06-landing-pages/about/mission.html` exits 0
    - `grep "G-XXXXXXXXXX" 06-landing-pages/about/mission.html` exits 0
    - `grep "calendly\\.event_scheduled" 06-landing-pages/about/mission.html` exits 0
  </acceptance_criteria>
  <done>mission.html exists with page header, mission/vision/purpose blocks, 6 value cards in values-grid, 8 manifesto items, and closing CTA section. All text uses data-i18n attributes with mission_page.* keys. VANTIX_BASE is '..' and all paths use ../ prefix. Calendly popup and GA4 booking listener are present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all three detail pages</name>
  <files>06-landing-pages/services/observability.html, 06-landing-pages/services/fractional-sre.html, 06-landing-pages/about/mission.html</files>
  <action>
Human visual verification of all three detail pages built across Plans 02 and 03.

Start a local server: `cd 06-landing-pages && python3 -m http.server 8080`

Verify each page:

1. http://localhost:8080/services/observability.html
   - Dark background, shared nav at top, shared footer at bottom
   - Green pill badge "Observability as a Service"
   - 6 numbered deliverables with green number badges
   - 3 outcome cards, timeline with blue dots, closing CTA
   - Click "Book a Free Demo" opens Calendly popup
   - Language toggle switches ALL text to Spanish

2. http://localhost:8080/services/fractional-sre.html
   - Same layout as observability with amber accents
   - Pill "Fractional SRE", price "$5,500/month"
   - Language toggle and Calendly popup work

3. http://localhost:8080/about/mission.html
   - Mission block (dark card), vision block (outlined), purpose block (blue tint)
   - 6 value cards in 2-column grid with numbered labels 01-06
   - 8 manifesto items with dash markers
   - CTA with Calendly popup, language toggle works

4. Cross-page test: switch to ES on index.html, navigate to detail page — shows Spanish
  </action>
  <verify>
    <automated>test -f 06-landing-pages/services/observability.html && test -f 06-landing-pages/services/fractional-sre.html && test -f 06-landing-pages/about/mission.html && echo "PASS: all 3 detail pages exist"</automated>
  </verify>
  <done>User has visually confirmed all three detail pages render correctly with dark theme, working i18n toggle, and functional Calendly popup CTAs.</done>
</task>

</tasks>

<verification>
1. `about/mission.html` exists with all required sections
2. Mission, Vision, Purpose blocks use distinct CSS classes (mission-block, vision-block, purpose-block)
3. Values grid has 6 cards with numeric indexes 01-06
4. Manifesto has 8 list items with dash markers
5. CTA section has working Calendly trigger button
6. All text uses data-i18n attributes with mission_page.* keys
7. Visual verification confirms all three pages render correctly with dark theme
</verification>

<success_criteria>
- Opening `http://localhost:8080/about/mission.html` shows dark-themed page with nav, footer, mission/vision/purpose blocks, values grid, manifesto, and CTA
- Toggling language switches all visible text
- Clicking "Book a Free Demo" opens Calendly popup
- Human verification confirms visual quality across all three detail pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-detail-pages/03-03-SUMMARY.md`
</output>
