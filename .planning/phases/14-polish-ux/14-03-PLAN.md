---
phase: 14-polish-ux
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - platform/src/app/[locale]/admin/page.tsx
  - platform/src/app/[locale]/admin/clients/page.tsx
  - platform/src/app/[locale]/admin/tasks/page.tsx
  - platform/src/app/[locale]/admin/billing/page.tsx
  - platform/src/app/[locale]/admin/users/page.tsx
autonomous: true
requirements:
  - UX-01
  - UX-03

must_haves:
  truths:
    - "Every admin page shows a skeleton while data is loading"
    - "If an admin page section fails to render, the error boundary catches it and the rest stays functional"
    - "Error boundary retry button re-renders the failed section"
  artifacts:
    - path: "platform/src/app/[locale]/admin/page.tsx"
      provides: "Admin overview with skeleton and error boundaries"
      contains: "SectionErrorBoundary"
    - path: "platform/src/app/[locale]/admin/clients/page.tsx"
      provides: "Admin clients with skeleton and error boundary"
      contains: "SectionErrorBoundary"
    - path: "platform/src/app/[locale]/admin/tasks/page.tsx"
      provides: "Admin tasks with skeleton and error boundary"
      contains: "SectionErrorBoundary"
    - path: "platform/src/app/[locale]/admin/billing/page.tsx"
      provides: "Admin billing with skeleton and error boundaries"
      contains: "SectionErrorBoundary"
    - path: "platform/src/app/[locale]/admin/users/page.tsx"
      provides: "Admin users with skeleton and error boundary"
      contains: "SectionErrorBoundary"
  key_links:
    - from: "all admin pages"
      to: "@/components/skeletons"
      via: "import + loading state conditional render"
      pattern: "SkeletonCard|SkeletonTable|SkeletonText"
    - from: "all admin pages"
      to: "@/components/SectionErrorBoundary"
      via: "import + wrapping data sections"
      pattern: "SectionErrorBoundary"
---

<objective>
Wire loading skeletons and error boundaries into all 5 admin pages.

Purpose: Admin pages currently render empty content while data loads. After this plan, every admin page shows skeleton placeholders during loading and has error boundaries around data sections.
Output: 5 updated admin page files with skeleton loading states and SectionErrorBoundary wrapping.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-ux/14-CONTEXT.md
@.planning/phases/14-polish-ux/14-RESEARCH.md
@.planning/phases/14-polish-ux/14-UI-SPEC.md
@.planning/phases/14-polish-ux/14-01-SUMMARY.md

@platform/src/app/[locale]/admin/page.tsx
@platform/src/app/[locale]/admin/clients/page.tsx
@platform/src/app/[locale]/admin/tasks/page.tsx
@platform/src/app/[locale]/admin/billing/page.tsx
@platform/src/app/[locale]/admin/users/page.tsx

<interfaces>
<!-- Components created by 14-01 -->

From platform/src/components/skeletons.tsx:
```typescript
export function SkeletonCard(): JSX.Element
export function SkeletonTable({ rows, cols }: { rows?: number; cols?: number }): JSX.Element
export function SkeletonText({ lines, className }: { lines?: number; className?: string }): JSX.Element
export function SkeletonChart({ height }: { height?: number }): JSX.Element
```

From platform/src/components/SectionErrorBoundary.tsx:
```typescript
export class SectionErrorBoundary extends React.Component<{
  children: React.ReactNode;
  fallbackTitle?: string;
  fallbackBody?: string;
  fallbackRetry?: string;
}, { hasError: boolean }>
```

i18n keys available:
```
common.error_section = "Something went wrong in this section"
common.error_section_body = "This section couldn't load. The rest of the page is still available."
common.error_retry = "Try again"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeletons + error boundaries to Admin Overview and Clients</name>
  <files>
    platform/src/app/[locale]/admin/page.tsx,
    platform/src/app/[locale]/admin/clients/page.tsx
  </files>
  <read_first>
    platform/src/app/[locale]/admin/page.tsx,
    platform/src/app/[locale]/admin/clients/page.tsx,
    platform/src/components/skeletons.tsx,
    platform/src/components/SectionErrorBoundary.tsx,
    .planning/phases/14-polish-ux/14-UI-SPEC.md
  </read_first>
  <action>
**Admin Overview (`platform/src/app/[locale]/admin/page.tsx`)** -- per D-02, D-03, D-12:

1. Add imports:
```typescript
import { SkeletonCard, SkeletonText } from '@/components/skeletons'
import { SectionErrorBoundary } from '@/components/SectionErrorBoundary'
```

2. Add `const [loading, setLoading] = useState(true)` after existing state declarations. Add `const tc = useTranslations('common')`.

3. Change the `load()` call (currently just `load()` at the end of useEffect) to `load().finally(() => setLoading(false))`.

4. Add skeleton loading branch before the return statement:
```typescript
if (loading) return (
  <div>
    <div className="h-8 bg-gray-200 rounded w-48 mb-6 animate-pulse" />
    <div className="flex gap-4 mb-6 flex-wrap">
      <SkeletonCard /><SkeletonCard /><SkeletonCard /><SkeletonCard />
    </div>
    <div className="bg-white rounded-xl border border-gray-100 p-6">
      <SkeletonText lines={5} />
    </div>
  </div>
)
```

5. Wrap the stat cards row (`<div className="flex gap-4 mb-6 flex-wrap">` containing 4 StatCards) in one SectionErrorBoundary:
```tsx
<SectionErrorBoundary fallbackTitle={tc('error_section')} fallbackBody={tc('error_section_body')} fallbackRetry={tc('error_retry')}>
  <div className="flex gap-4 mb-6 flex-wrap">
    <StatCard ... /><StatCard ... /><StatCard ... /><StatCard ... />
  </div>
</SectionErrorBoundary>
```

6. Wrap the activity feed (`<div className="bg-white rounded-xl border border-gray-100 p-6">` containing the activity list) in a separate SectionErrorBoundary.

**Admin Clients (`platform/src/app/[locale]/admin/clients/page.tsx`)** -- per D-02, D-03, D-12:

1. Add imports:
```typescript
import { SkeletonTable } from '@/components/skeletons'
import { SectionErrorBoundary } from '@/components/SectionErrorBoundary'
```

2. Add `const [loading, setLoading] = useState(true)` after existing state. Add `const tc = useTranslations('common')`.

3. Change `load()` call to `load().finally(() => setLoading(false))`. The admin clients page has a `useEffect` with a `load()` function that fetches clients, subscriptions, and tasks. Find the bare `load()` call and wrap it.

4. Add skeleton branch before return:
```typescript
if (loading) return (
  <div>
    <div className="h-8 bg-gray-200 rounded w-40 mb-6 animate-pulse" />
    <SkeletonTable rows={5} cols={5} />
  </div>
)
```

5. Wrap the clients table (everything after the search input and before the page closing div) in a SectionErrorBoundary. Keep the search input OUTSIDE the boundary so it remains usable even if the table fails.
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Desktop/vantix-kit/platform && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - admin/page.tsx contains `import { SkeletonCard, SkeletonText } from '@/components/skeletons'`
    - admin/page.tsx contains `import { SectionErrorBoundary } from '@/components/SectionErrorBoundary'`
    - admin/page.tsx contains `const [loading, setLoading] = useState(true)`
    - admin/page.tsx contains `load().finally(() => setLoading(false))`
    - admin/page.tsx contains `if (loading) return`
    - admin/page.tsx contains `<SectionErrorBoundary` (at least 2 occurrences)
    - admin/clients/page.tsx contains `import { SkeletonTable } from '@/components/skeletons'`
    - admin/clients/page.tsx contains `const [loading, setLoading] = useState(true)`
    - admin/clients/page.tsx contains `load().finally`
    - admin/clients/page.tsx contains `<SectionErrorBoundary`
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>
    Admin overview shows skeleton while loading and has 2 error boundaries (stats row, activity feed). Admin clients shows skeleton table while loading and has error boundary around the clients table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add skeletons + error boundaries to Admin Tasks, Billing, Users</name>
  <files>
    platform/src/app/[locale]/admin/tasks/page.tsx,
    platform/src/app/[locale]/admin/billing/page.tsx,
    platform/src/app/[locale]/admin/users/page.tsx
  </files>
  <read_first>
    platform/src/app/[locale]/admin/tasks/page.tsx,
    platform/src/app/[locale]/admin/billing/page.tsx,
    platform/src/app/[locale]/admin/users/page.tsx,
    platform/src/components/skeletons.tsx,
    platform/src/components/SectionErrorBoundary.tsx,
    .planning/phases/14-polish-ux/14-UI-SPEC.md
  </read_first>
  <action>
**Admin Tasks (`platform/src/app/[locale]/admin/tasks/page.tsx`)** -- per D-02, D-03, D-12:

1. Add imports:
```typescript
import { SkeletonTable } from '@/components/skeletons'
import { SectionErrorBoundary } from '@/components/SectionErrorBoundary'
```

2. Add `const [loading, setLoading] = useState(true)` after existing state. Add `const tc = useTranslations('common')`.

3. Change `load()` call to `load().finally(() => setLoading(false))`.

4. Add skeleton branch before return:
```typescript
if (loading) return (
  <div>
    <div className="h-8 bg-gray-200 rounded w-40 mb-6 animate-pulse" />
    <SkeletonTable rows={6} cols={6} />
  </div>
)
```

5. Wrap the tasks table in a SectionErrorBoundary. Keep filter dropdowns OUTSIDE the boundary.

**Admin Billing (`platform/src/app/[locale]/admin/billing/page.tsx`)** -- per D-02, D-03, D-12:

1. Add imports:
```typescript
import { SkeletonCard, SkeletonText, SkeletonChart } from '@/components/skeletons'
import { SectionErrorBoundary } from '@/components/SectionErrorBoundary'
```

2. Add `const [loading, setLoading] = useState(true)` after existing state. Add `const tc = useTranslations('common')`.

3. Change `load()` call to `load().finally(() => setLoading(false))`.

4. Add skeleton branch before return:
```typescript
if (loading) return (
  <div>
    <div className="h-8 bg-gray-200 rounded w-48 mb-6 animate-pulse" />
    <div className="flex gap-4 mb-6 flex-wrap">
      <SkeletonCard /><SkeletonCard /><SkeletonCard /><SkeletonCard />
    </div>
    <SkeletonChart height={200} />
    <div className="mt-6"><SkeletonText lines={4} /></div>
    <div className="mt-6"><SkeletonText lines={4} /></div>
  </div>
)
```

5. Wrap the stat cards row in one SectionErrorBoundary. Wrap the MRR chart in a separate SectionErrorBoundary. These are the two boundaries specified in the UI-SPEC for admin billing. The payments table and subscriptions table below are less critical but wrap them in a third combined boundary for safety.

**Admin Users (`platform/src/app/[locale]/admin/users/page.tsx`)** -- per D-02, D-03, D-12:

1. Add imports:
```typescript
import { SkeletonTable } from '@/components/skeletons'
import { SectionErrorBoundary } from '@/components/SectionErrorBoundary'
```

2. Add `const [loading, setLoading] = useState(true)` after existing state. Add `const tc = useTranslations('common')`.

3. Change `load()` call to `load().finally(() => setLoading(false))`.

4. Add skeleton branch before return:
```typescript
if (loading) return (
  <div>
    <div className="h-8 bg-gray-200 rounded w-56 mb-6 animate-pulse" />
    <SkeletonTable rows={5} cols={7} />
  </div>
)
```

5. Wrap the users table in a SectionErrorBoundary. Keep the invite button, search input, and any form OUTSIDE the boundary. The invite form (if it's a modal or inline form) should be outside the table boundary since it has independent state.
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Desktop/vantix-kit/platform && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - admin/tasks/page.tsx contains `import { SkeletonTable } from '@/components/skeletons'`
    - admin/tasks/page.tsx contains `const [loading, setLoading] = useState(true)`
    - admin/tasks/page.tsx contains `load().finally`
    - admin/tasks/page.tsx contains `<SectionErrorBoundary`
    - admin/billing/page.tsx contains `import { SkeletonCard, SkeletonText, SkeletonChart } from '@/components/skeletons'`
    - admin/billing/page.tsx contains `const [loading, setLoading] = useState(true)`
    - admin/billing/page.tsx contains `load().finally`
    - admin/billing/page.tsx contains `<SectionErrorBoundary` (at least 2 occurrences)
    - admin/users/page.tsx contains `import { SkeletonTable } from '@/components/skeletons'`
    - admin/users/page.tsx contains `const [loading, setLoading] = useState(true)`
    - admin/users/page.tsx contains `load().finally`
    - admin/users/page.tsx contains `<SectionErrorBoundary`
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>
    Admin tasks page shows skeleton table while loading with error boundary. Admin billing shows skeleton with cards + chart + text while loading with 2+ error boundaries. Admin users shows skeleton table while loading with error boundary around users table.
  </done>
</task>

</tasks>

<verification>
- `cd platform && npx tsc --noEmit` exits 0
- All 5 admin pages import and use SectionErrorBoundary
- All 5 admin pages show skeleton placeholders during data loading
- Admin billing has separate error boundaries for stat cards, MRR chart, and tables
- Search inputs and filter dropdowns remain outside error boundaries
</verification>

<success_criteria>
- Every admin page that fetches data shows a skeleton placeholder while loading (per D-02)
- Every skeleton disappears when data arrives, using `finally { setLoading(false) }` (per Pitfall 2)
- Each major data section on admin pages is wrapped in SectionErrorBoundary (per D-12)
- Rest of page stays functional when one section fails (per D-14)
- TypeScript builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-polish-ux/14-03-SUMMARY.md`
</output>
