---
phase: 04-polish-launch-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/css/landing.css
  - landing/css/base.css
  - landing/index.html
  - landing/i18n/en.json
  - landing/i18n/es.json
autonomous: true
requirements: [DSNG-02]

must_haves:
  truths:
    - "Credibility bar appears without visible pop-in delay on fast connections"
    - "Hero section on desktop has no excessive empty whitespace below content"
    - "All borders on the site are solid — no dashed border outliers"
    - "No visible layout jumps (CLS) when scrolling through sections with content-visibility"
    - "Also Available rows have a single 'See all services' link instead of 3 identical Book a Call buttons"
    - "LATAM banner text is left-aligned to match page rhythm"
  artifacts:
    - path: "landing/css/landing.css"
      provides: "Credibility bar fade-in, hero desktop override, dashed-to-solid border, calibrated contain-intrinsic-size, LATAM left-align, removed per-row CTA styles"
      contains: "fade-in 400ms"
    - path: "landing/css/base.css"
      provides: "Calibrated FAQ contain-intrinsic-size"
      contains: "contain-intrinsic-size"
    - path: "landing/index.html"
      provides: "Single 'See all services' link replacing 3 per-row Book a Call buttons"
      contains: "services.see_all"
  key_links:
    - from: "landing/css/landing.css"
      to: ".credibility in index.html"
      via: "CSS animation rule"
      pattern: "fade-in 400ms"
    - from: "landing/index.html"
      to: "landing/i18n/en.json"
      via: "data-i18n attribute"
      pattern: "services.see_all"
---

<objective>
Fix all CSS bugs and layout issues identified in todo.md and CONTEXT.md decisions D-04, D-05, D-15, D-16, and the LATAM banner/dashed border issues.

Purpose: These are visual quality gates that must pass before the impeccable.style audit (DSNG-02). Fixing them first means the audit in Plan 03 sees a clean baseline rather than re-flagging known issues.

Output: Clean CSS with no pop-in animations, no excessive hero whitespace, no dashed border outliers, calibrated content-visibility sizes, left-aligned LATAM banner, and a single services link replacing 3 identical CTAs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-launch-gate/04-CONTEXT.md
@.planning/phases/04-polish-launch-gate/04-UI-SPEC.md

<interfaces>
<!-- Key CSS rules being modified — executor must see current state -->

From landing/css/landing.css line 162-164:
```css
.credibility {
  border-bottom: 1px solid var(--color-border);
  animation: hero-enter 600ms var(--ease-out) 700ms both;
}
```

From landing/css/landing.css line 11-18:
```css
.hero {
  position: relative;
  min-height: calc(100vh - var(--nav-height));
  min-height: calc(100dvh - var(--nav-height));
  display: flex;
  align-items: center;
  overflow: hidden;
  background: var(--color-surface);
}
```

From landing/css/landing.css line 709-712:
```css
.svc-card__onetime {
  margin-top: var(--space-6);
  padding-top: var(--space-6);
  border-top: 1px dashed var(--color-border);
}
```

From landing/css/landing.css line 682-688:
```css
.latam-banner {
  background: var(--color-accent-subtle);
  border: 1px solid var(--color-accent-border);
  border-radius: var(--radius-lg);
  padding: var(--space-6) var(--space-8);
  text-align: center;
  margin-top: var(--space-10);
}
```

content-visibility sections:
- `.compare` line 191-192: `contain-intrinsic-size: auto 500px`
- `.pain` line 344-345: `contain-intrinsic-size: auto 700px`
- `.cta-section` line 828-829: `contain-intrinsic-size: auto 500px`
- `.faq` in base.css line 698-699: `contain-intrinsic-size: auto 600px`

Also Available HTML (index.html lines 352-378):
3 `<li>` elements each with `<button class="btn btn--outline btn--sm js-calendly-trigger">Book a Call</button>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CSS bugs — credibility bar, hero height, dashed border, CLS, LATAM banner</name>
  <files>landing/css/landing.css, landing/css/base.css</files>
  <read_first>
    - landing/css/landing.css (full file — multiple edits across lines 11-18, 162-164, 191-192, 344-345, 682-688, 709-712, 828-829)
    - landing/css/base.css (lines 690-700 — FAQ content-visibility)
    - landing/css/detail.css (lines 396-397, 485-486 — detail page content-visibility for reference, but NOT modified in this task)
    - .planning/phases/04-polish-launch-gate/04-UI-SPEC.md (interaction contracts section)
  </read_first>
  <action>
Make 6 targeted CSS edits:

**1. Credibility bar animation (D-04)** — landing.css line 164:
Change:
```css
animation: hero-enter 600ms var(--ease-out) 700ms both;
```
To:
```css
animation: fade-in 400ms var(--ease-out) both;
```
Add the `fade-in` keyframe AFTER the existing `hero-enter` keyframe (keep `hero-enter` intact for other uses):
```css
@keyframes fade-in {
  from { opacity: 0; }
}
```

**2. Hero height desktop override (D-05)** — landing.css, add AFTER the `.hero` rule block (after line ~18):
```css
@media (min-width: 1024px) {
  .hero {
    min-height: 0;
    padding-top: var(--space-24);
    padding-bottom: var(--space-24);
  }
}
```
Do NOT remove the mobile `min-height: calc(100dvh - var(--nav-height))` rule — it is correct for mobile.

**3. Dashed border fix** — landing.css line 712:
Change:
```css
border-top: 1px dashed var(--color-border);
```
To:
```css
border-top: 1px solid var(--color-border);
```

**4. CLS contain-intrinsic-size calibration (D-15)** — Update estimates to better match actual rendered heights. These values are approximate — the `auto` prefix means the browser learns the real height after first paint:
- landing.css `.compare` line 192: change `auto 500px` to `auto 520px`
- landing.css `.pain` line 345: keep `auto 700px` (already reasonable)
- landing.css `.cta-section` line 829: change `auto 500px` to `auto 380px` (CTA sections are shorter — padding + small content)
- base.css `.faq` line 699: keep `auto 600px` (reasonable for 5 FAQ items)

Note: These are starting estimates. The checkpoint in Plan 03 will verify actual sizes and flag any remaining CLS issues.

**5. LATAM banner alignment** — landing.css line 687:
Change:
```css
text-align: center;
```
To:
```css
text-align: left;
```

**6. Remove per-row CTA button styles for Also Available** — landing.css around lines 481-487 (inside `@media (min-width: 768px)` block):
The `.services__also-row .btn` rules can remain in CSS (they'll be unused), or add a comment `/* btn removed from HTML — see D-16 */`. No CSS deletion needed since the HTML change in Task 2 removes the buttons.
  </action>
  <verify>
    <automated>grep -n "fade-in 400ms" landing/css/landing.css && grep -n "min-height: 0" landing/css/landing.css && grep -c "dashed" landing/css/landing.css && grep -n "text-align: left" landing/css/landing.css | grep "latam" -i</automated>
  </verify>
  <acceptance_criteria>
    - landing/css/landing.css contains `animation: fade-in 400ms var(--ease-out) both;` on the `.credibility` rule
    - landing/css/landing.css contains `@keyframes fade-in` with `from { opacity: 0; }`
    - landing/css/landing.css contains `@media (min-width: 1024px)` block with `.hero { min-height: 0;`
    - landing/css/landing.css line for `.svc-card__onetime` contains `border-top: 1px solid var(--color-border)` (not dashed)
    - `grep -c "dashed" landing/css/landing.css` returns 0
    - landing/css/landing.css `.latam-banner` contains `text-align: left`
    - landing/css/landing.css `.compare` contains `contain-intrinsic-size: auto 520px`
    - landing/css/landing.css `.cta-section` contains `contain-intrinsic-size: auto 380px`
    - The original `.hero` mobile rules (`min-height: calc(100dvh - var(--nav-height))`) are still present
    - The `@keyframes hero-enter` keyframe is still present (not removed)
  </acceptance_criteria>
  <done>All 6 CSS fixes applied. Credibility bar fades in without pop-in. Hero desktop has no excess whitespace. No dashed borders remain. CLS estimates calibrated. LATAM banner left-aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Also Available per-row CTAs with single "See all services" link (D-16)</name>
  <files>landing/index.html, landing/i18n/en.json, landing/i18n/es.json</files>
  <read_first>
    - landing/index.html (lines 350-380 — the services__also section)
    - landing/i18n/en.json (check for existing `services.see_all` key — does not exist yet)
    - landing/i18n/es.json (same check)
    - .planning/phases/04-polish-launch-gate/04-UI-SPEC.md (interaction contracts — "Also Available" CTA Row section)
  </read_first>
  <action>
**HTML change in index.html** — In the `.services__also` block (lines 352-378):

1. Remove the 3 `<button class="btn btn--outline btn--sm js-calendly-trigger" type="button" data-i18n="hero.cta_primary">Book a Call</button>` elements from each `<li>` (lines 361, 368, 375). Each `<li>` should retain only the `.services__also-info` div with the service name link and description.

2. After the closing `</ul>` tag of `.services__also-list` (line 377), add:
```html
          <p class="services__also-footer">
            <a href="#services" class="services__also-link" data-i18n="services.see_all">See all services &rarr;</a>
          </p>
```

3. Add CSS for the new footer element in landing.css, after the `.services__also-desc` rule (~line 478):
```css
.services__also-footer {
  margin-top: var(--space-4);
  padding-top: var(--space-4);
  border-top: 1px solid var(--color-border);
}

.services__also-link {
  font-size: var(--text-sm);
  color: var(--color-text-muted);
  text-decoration: none;
  transition: color var(--transition-fast);
}

.services__also-link:hover {
  color: var(--color-accent);
}

.services__also-link:focus-visible {
  outline: 2px solid var(--color-border-focus);
  outline-offset: 2px;
  border-radius: var(--radius-sm);
}
```

**i18n changes:**

Add to en.json inside the `"services"` object:
```json
"see_all": "See all services \u2192"
```

Add to es.json inside the `"services"` object:
```json
"see_all": "Ver todos los servicios \u2192"
```
  </action>
  <verify>
    <automated>grep -c "js-calendly-trigger" landing/index.html && grep "services.see_all" landing/index.html && grep "see_all" landing/i18n/en.json && grep "see_all" landing/i18n/es.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "js-calendly-trigger" landing/index.html` returns a number that is 3 LESS than before (the 3 per-row buttons removed; other Calendly triggers on the page remain)
    - landing/index.html contains `data-i18n="services.see_all"` inside a `.services__also-footer` paragraph
    - landing/index.html does NOT contain a `<button>` element inside any `.services__also-row` element
    - landing/i18n/en.json contains `"see_all": "See all services` inside the `services` object
    - landing/i18n/es.json contains `"see_all": "Ver todos los servicios` inside the `services` object
    - landing/css/landing.css contains `.services__also-footer` and `.services__also-link` rules
    - The `.services__also-row` `<li>` elements still contain their `<a>` service name links and `<span>` descriptions (only the button was removed)
  </acceptance_criteria>
  <done>3 identical "Book a Call" buttons removed from Also Available rows. Single "See all services" text link added below the group. i18n keys added for both EN and ES.</done>
</task>

</tasks>

<verification>
- Open index.html in browser — credibility bar should fade in smoothly without pop-in
- On desktop (>1024px), hero section should fit content without excessive empty whitespace below
- Service card one-time section border should be solid, not dashed
- LATAM banner text should be left-aligned
- Also Available section should show 3 service rows with descriptions but NO per-row CTA buttons, and a single "See all services" link at the bottom
- Scroll through the page — no visible layout jumps from content-visibility sections
</verification>

<success_criteria>
All CSS bugs from todo.md resolved. `grep -c "dashed" landing/css/landing.css` returns 0. Credibility bar uses `fade-in` animation. Hero has desktop `min-height: 0` override. LATAM banner is `text-align: left`. Also Available rows have no per-row CTA buttons. CLS estimates calibrated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-launch-gate/04-01-SUMMARY.md`
</output>
