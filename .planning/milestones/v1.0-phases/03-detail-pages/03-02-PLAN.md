---
phase: 03-detail-pages
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - 06-landing-pages/services/observability.html
  - 06-landing-pages/services/fractional-sre.html
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02

must_haves:
  truths:
    - "services/observability.html loads with dark theme, shared nav, shared footer"
    - "services/fractional-sre.html loads with dark theme, shared nav, shared footer"
    - "Language toggle switches all text on both service pages"
    - "Clicking CTA buttons on both pages opens Calendly popup"
    - "Both pages show canonical pricing ($3,000/month observability, $5,500/month SRE)"
  artifacts:
    - path: "06-landing-pages/services/observability.html"
      provides: "Observability as a Service detail page with hero, deliverables, outcomes, timeline, CTA"
      min_lines: 200
    - path: "06-landing-pages/services/fractional-sre.html"
      provides: "Fractional SRE detail page with hero, deliverables, outcomes, timeline, CTA"
      min_lines: 200
  key_links:
    - from: "06-landing-pages/services/observability.html"
      to: "06-landing-pages/i18n/en.json"
      via: "data-i18n attributes referencing obs_page.* keys"
      pattern: "data-i18n=\"obs_page\\."
    - from: "06-landing-pages/services/fractional-sre.html"
      to: "06-landing-pages/i18n/en.json"
      via: "data-i18n attributes referencing sre_page.* keys"
      pattern: "data-i18n=\"sre_page\\."
    - from: "06-landing-pages/services/observability.html"
      to: "06-landing-pages/js/partials.js"
      via: "data-partial nav and footer divs + VANTIX_BASE = '..'"
      pattern: "data-partial=\"nav\""
    - from: "06-landing-pages/services/fractional-sre.html"
      to: "06-landing-pages/js/partials.js"
      via: "data-partial nav and footer divs + VANTIX_BASE = '..'"
      pattern: "data-partial=\"nav\""
    - from: "06-landing-pages/services/observability.html"
      to: "06-landing-pages/css/detail.css"
      via: "CSS link in head"
      pattern: "detail\\.css"
    - from: "06-landing-pages/services/fractional-sre.html"
      to: "06-landing-pages/css/detail.css"
      via: "CSS link in head"
      pattern: "detail\\.css"
---

<objective>
Create the two service detail pages: `services/observability.html` (PAGE-01) and `services/fractional-sre.html` (PAGE-02).

Purpose: Deep-evaluation buyers clicking "Learn More" from the main landing service cards arrive at these pages. Each page provides detailed deliverables, outcomes, a timeline, and a Calendly conversion CTA — all bilingual.

Output: Two complete HTML files in `06-landing-pages/services/` following the exact boilerplate from `index.html` with `VANTIX_BASE = '..'`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detail-pages/03-RESEARCH.md
@.planning/phases/03-detail-pages/03-UI-SPEC.md
@.planning/phases/03-detail-pages/03-01-SUMMARY.md
@06-landing-pages/index.html
@06-landing-pages/css/detail.css
@06-landing-pages/i18n/en.json
@06-landing-pages/i18n/es.json

<interfaces>
<!-- Key boilerplate from index.html that must be replicated exactly (with path adjustments) -->

HTML shell structure (from index.html):
- DOCTYPE, html lang="en", head with charset, viewport, description, title
- Google Fonts preconnect + link (DM Sans + JetBrains Mono)
- CSS links: tokens.css, base.css, [page CSS], Calendly widget.css
- GA4 snippet in head (same G-XXXXXXXXXX placeholder)
- body > div[data-partial="nav"] > main#main-content > div[data-partial="footer"]
- Scripts: VANTIX_BASE, i18n.js, partials.js, Calendly widget.js (async), Calendly popup trigger, GA4 booking event listener

Key difference for nested pages:
- window.VANTIX_BASE = '..' (not '.')
- All CSS/JS paths use ../ prefix (../css/tokens.css, ../js/i18n.js, etc.)

CTA section HTML (reused on both pages, from index.html lines 314-335):
```html
<section class="cta-section section" aria-labelledby="page-cta-heading">
  <div class="container">
    <div class="cta-section__inner">
      <h2 id="page-cta-heading" class="cta-section__heading" data-i18n="cta_section.heading">Ready to stop guessing?</h2>
      <p class="cta-section__sub" data-i18n="cta_section.subheading">...</p>
      <div class="cta-section__actions">
        <button class="btn btn--primary btn--lg js-calendly-trigger" type="button" data-i18n="cta_section.cta_primary">Book a Free Demo</button>
      </div>
    </div>
  </div>
</section>
```

CSS classes available from detail.css (created in Plan 01):
- .page-hero, .page-hero--observability, .page-hero--sre
- .page-hero__inner, .page-hero__pill, .page-hero__headline, .page-hero__headline-accent
- .page-hero__sub, .page-hero__actions, .page-hero__price, .page-hero__price-amount, .page-hero__price-period
- .deliverables__intro, .deliverable, .deliverable__num, .deliverable__num--green, .deliverable__num--amber
- .deliverable__title, .deliverable__body, .deliverable__badges, .deliverable__badge, .deliverable__badge--freq-green, .deliverable__badge--freq-amber
- .outcomes-grid, .outcome-card, .outcome-card__title, .outcome-card__body
- .timeline, .timeline__step, .timeline__step-title, .timeline__step-body, .timeline__badge
- .cta-section (from landing.css — reused)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create services/observability.html (PAGE-01)</name>
  <files>06-landing-pages/services/observability.html</files>
  <read_first>
    06-landing-pages/index.html
    06-landing-pages/css/detail.css
    06-landing-pages/i18n/en.json
    .planning/phases/03-detail-pages/03-UI-SPEC.md
    .planning/phases/03-detail-pages/03-RESEARCH.md
  </read_first>
  <action>
Create `06-landing-pages/services/observability.html`. This is a complete HTML page following the index.html boilerplate exactly, with `VANTIX_BASE = '..'` and `../` prefixed paths.

**HEAD section** (copy from index.html, adjust paths):
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" data-i18n-attr="content:obs_page.meta_description" content="Managed observability — we instrument your systems, build dashboards, tune alerts, and optimize telemetry costs. Continuously improving, not a one-off project.">
  <title data-i18n="obs_page.meta_title">Observability as a Service — Vantix</title>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">

  <!-- Design system — paths use ../ because this page is one level deep -->
  <link rel="stylesheet" href="../css/tokens.css">
  <link rel="stylesheet" href="../css/base.css">
  <link rel="stylesheet" href="../css/detail.css">

  <!-- Calendly widget CSS -->
  <link href="https://assets.calendly.com/assets/external/widget.css" rel="stylesheet">

  <!-- Google Analytics 4 — REPLACE G-XXXXXXXXXX with your real Measurement ID before launch -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag() { dataLayer.push(arguments); }
    gtag('js', new Date());
    gtag('config', 'G-XXXXXXXXXX');
  </script>
</head>
```

**BODY structure:**
```html
<body>
  <div data-partial="nav"></div>

  <main id="main-content" tabindex="-1">
    [sections below]
  </main>

  <div data-partial="footer"></div>

  <!-- Scripts — load order matters -->
  <script>window.VANTIX_BASE = '..';</script>
  <script src="../js/i18n.js"></script>
  <script src="../js/partials.js"></script>

  <!-- Calendly widget JS (async) -->
  <script src="https://assets.calendly.com/assets/external/widget.js" type="text/javascript" async></script>

  <!-- Calendly popup trigger + GA4 booking event — identical to index.html -->
  <script>
    var CALENDLY_URL = 'https://calendly.com/vantix/30min';
    document.addEventListener('click', function (e) {
      var trigger = e.target.closest('.js-calendly-trigger, [href="#calendly"]');
      if (!trigger) return;
      e.preventDefault();
      if (window.Calendly) {
        Calendly.initPopupWidget({ url: CALENDLY_URL });
      } else {
        window.open(CALENDLY_URL, '_blank', 'noopener,noreferrer');
      }
    });
    window.addEventListener('message', function (e) {
      if (e.data && e.data.event === 'calendly.event_scheduled') {
        if (typeof gtag === 'function') {
          gtag('event', 'calendly_booking', {
            event_category: 'engagement',
            event_label: 'demo_scheduled'
          });
        }
      }
    });
  </script>
</body>
</html>
```

**MAIN sections** (inside `<main>`):

**Section 1: Page Hero** — Use `.page-hero.page-hero--observability` and `.section` classes.
- Pill badge: `<div class="pill" data-i18n="obs_page.hero.pill">Observability as a Service</div>` (the `.page-hero--observability .pill` CSS handles the green color)
- Headline h1 with `.page-hero__headline`: `<span data-i18n="obs_page.hero.headline">` + `<span class="page-hero__headline-accent" data-i18n="obs_page.hero.headline_accent">`
- Subheadline p with `.page-hero__sub` and `data-i18n="obs_page.hero.subheadline"`
- Price display: `<div class="page-hero__price"><span class="page-hero__price-amount" data-i18n="services.observability.price">$3,000</span><span class="page-hero__price-period" data-i18n="services.observability.price_period">/month</span></div>` (reuse existing keys)
- Actions div with `.page-hero__actions`: primary CTA button `<button class="btn btn--primary btn--lg js-calendly-trigger" type="button" data-i18n="cta_section.cta_primary">Book a Free Demo</button>` + outline link back `<a href="../#services" class="btn btn--outline btn--lg" data-i18n="hero.cta_secondary">View Services</a>`
- Add `aria-label="Hero"` to the section

**Section 2: Problem** — Simple section with `.section` class, `background: var(--color-surface)` via inline class or a wrapper.
- Two `<p>` tags with `data-i18n="obs_page.problem.body_1"` and `data-i18n="obs_page.problem.body_2"`. Use `.deliverables__intro` class for styling (same font size/color) or add a simple `.problem-section` wrapper.
- Actually, use a simple `<section class="section" style="background: var(--color-surface);">` wrapper with a `.container` div. Each `<p>` gets `style="..."` — NO, no inline styles. Instead, use the `.section-sub` class from base.css for the paragraphs (it already has the right font size and color). Set `max-width: none` via a wrapper class. OR better: just use `<p>` tags inside `.container` — the body text styling from base.css reset is sufficient with the `.deliverables__intro` class on each paragraph.
- Simpler approach: wrap in a `<section class="section" id="problem">` with background handled via a simple additional class `.bg-surface { background: var(--color-surface); }` added to detail.css. OR just use an inline data attribute. BEST approach: add the problem section paragraphs as `.deliverables__intro` styled elements (they have the right `font-size: var(--text-base); color: var(--color-text-muted);` styling). No extra CSS class needed.

Use this structure:
```html
<section class="section" id="problem" style="background: var(--color-surface);">
  <div class="container">
    <p class="deliverables__intro" data-i18n="obs_page.problem.body_1">...</p>
    <p class="deliverables__intro" data-i18n="obs_page.problem.body_2" style="margin-bottom: 0;">...</p>
  </div>
</section>
```

NOTE: The two inline styles above (`background` on section, `margin-bottom: 0` on last p) are acceptable as layout overrides — they are not color or font values. If the executor prefers, add a `.bg-surface` utility class to detail.css instead.

**Section 3: Deliverables** — `<section class="section" id="deliverables" aria-labelledby="deliverables-heading">`
- Section heading: `<div class="section-header"><h2 id="deliverables-heading" class="section-title" data-i18n="obs_page.deliverables.heading">What our clients receive</h2></div>`
- Intro paragraph: `<p class="deliverables__intro" data-i18n="obs_page.deliverables.intro">...</p>`
- 6 deliverable items, each structured as:
```html
<div class="deliverable">
  <div class="deliverable__num deliverable__num--green">1</div>
  <div>
    <h3 class="deliverable__title" data-i18n="obs_page.deliverables.items.0.title">...</h3>
    <p class="deliverable__body" data-i18n="obs_page.deliverables.items.0.body">...</p>
    <div class="deliverable__badges">
      <span class="deliverable__badge" data-i18n="obs_page.deliverables.items.0.badges.0">...</span>
      <span class="deliverable__badge" data-i18n="obs_page.deliverables.items.0.badges.1">...</span>
      <span class="deliverable__badge deliverable__badge--freq-green" data-i18n="obs_page.deliverables.items.0.freq">Weeks 1-3</span>
    </div>
  </div>
</div>
```
- Repeat for items 1-5, incrementing the number and array index. Items 0, 1 have a `.freq` badge. Item 2 has no freq badge. Item 3 has freq badges. Item 4 has a freq badge. Item 5 has freq badges (see legacy file for which items have tool vs freq badges).
- Use `deliverable__num--green` on all number badges (observability = green accent).

**Section 4: Outcomes** — `<section class="section" id="outcomes" style="background: var(--color-surface);" aria-labelledby="outcomes-heading">`
- Section heading: `.section-title` with `data-i18n="obs_page.outcomes.heading"`
- `.outcomes-grid` with 3 `.outcome-card` elements:
```html
<div class="outcomes-grid">
  <div class="outcome-card">
    <h4 class="outcome-card__title" data-i18n="obs_page.outcomes.items.0.title">Fewer surprises</h4>
    <p class="outcome-card__body" data-i18n="obs_page.outcomes.items.0.body">...</p>
  </div>
  <!-- items 1 and 2 -->
</div>
```

**Section 5: How It Works** — `<section class="section" id="how-it-works" aria-labelledby="how-heading">`
- Add `border-top: 1px solid var(--color-border)` via inline style or via the section
- Section heading: `.section-title` with `data-i18n="obs_page.how.heading"`
- `.timeline` wrapper with 6 `.timeline__step` elements:
```html
<div class="timeline">
  <div class="timeline__step">
    <h4 class="timeline__step-title">
      <span data-i18n="obs_page.how.steps.0.title">Kick-off</span>
      <span class="timeline__badge" data-i18n="obs_page.how.steps.0.badge">15 min video call</span>
    </h4>
    <p class="timeline__step-body" data-i18n="obs_page.how.steps.0.body">...</p>
  </div>
  <!-- steps 1-5 -->
</div>
```
- Steps without a badge omit the `.timeline__badge` span.

**Section 6: CTA** — Reuse the exact CTA section pattern from index.html (lines 314-335). Use `cta_section.*` keys. Use `page-cta-heading` as the id.

Every text-containing element MUST have a `data-i18n` attribute. NO hard-coded text anywhere in the page except HTML comments. The English fallback text in the HTML should match the `en.json` values.
  </action>
  <verify>
    <automated>test -f 06-landing-pages/services/observability.html && grep -c "data-i18n" 06-landing-pages/services/observability.html | awk '{if ($1 >= 30) print "PASS: " $1 " i18n attributes"; else print "FAIL: only " $1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f 06-landing-pages/services/observability.html` exits 0
    - `grep 'data-partial="nav"' 06-landing-pages/services/observability.html` exits 0
    - `grep 'data-partial="footer"' 06-landing-pages/services/observability.html` exits 0
    - `grep "VANTIX_BASE = '\\.\\.'" 06-landing-pages/services/observability.html` exits 0
    - `grep "js-calendly-trigger" 06-landing-pages/services/observability.html` exits 0
    - `grep "page-hero--observability" 06-landing-pages/services/observability.html` exits 0
    - `grep "obs_page\\.hero\\.pill" 06-landing-pages/services/observability.html` exits 0
    - `grep "obs_page\\.deliverables\\.items" 06-landing-pages/services/observability.html` exits 0
    - `grep "obs_page\\.outcomes\\.items" 06-landing-pages/services/observability.html` exits 0
    - `grep "obs_page\\.how\\.steps" 06-landing-pages/services/observability.html` exits 0
    - `grep "cta_section\\.heading" 06-landing-pages/services/observability.html` exits 0
    - `grep "cta_section\\.cta_primary" 06-landing-pages/services/observability.html` exits 0
    - `grep "\\.\\./css/tokens\\.css" 06-landing-pages/services/observability.html` exits 0
    - `grep "\\.\\./css/detail\\.css" 06-landing-pages/services/observability.html` exits 0
    - `grep "\\.\\./js/i18n\\.js" 06-landing-pages/services/observability.html` exits 0
    - `grep "\\.\\./js/partials\\.js" 06-landing-pages/services/observability.html` exits 0
    - `grep "G-XXXXXXXXXX" 06-landing-pages/services/observability.html` exits 0 (GA4 placeholder present)
    - `grep "calendly\\.event_scheduled" 06-landing-pages/services/observability.html` exits 0 (GA4 booking listener present)
    - `grep "services\\.observability\\.price" 06-landing-pages/services/observability.html` exits 0 (canonical pricing key used)
  </acceptance_criteria>
  <done>observability.html exists with page-hero, 6 deliverables, 3 outcomes, 6-step timeline, and CTA section. All text uses data-i18n attributes. VANTIX_BASE is '..' and all paths use ../ prefix. Calendly popup trigger and GA4 booking listener are present.</done>
</task>

<task type="auto">
  <name>Task 2: Create services/fractional-sre.html (PAGE-02)</name>
  <files>06-landing-pages/services/fractional-sre.html</files>
  <read_first>
    06-landing-pages/services/observability.html
    06-landing-pages/i18n/en.json
    .planning/phases/03-detail-pages/03-UI-SPEC.md
  </read_first>
  <action>
Create `06-landing-pages/services/fractional-sre.html`. This page follows the IDENTICAL structure as `observability.html` (just created in Task 1) with these differences:

**Head changes:**
- `<meta name="description" data-i18n-attr="content:sre_page.meta_description" content="...">`
- `<title data-i18n="sre_page.meta_title">Fractional SRE — Vantix</title>`
- All other head elements (fonts, CSS links, GA4) are IDENTICAL to observability.html

**Hero changes:**
- Section class: `page-hero page-hero--sre section` (amber accent instead of green)
- Pill: `data-i18n="sre_page.hero.pill"` with text "Fractional SRE"
- Headline: `data-i18n="sre_page.hero.headline"` + accent span `data-i18n="sre_page.hero.headline_accent"`
- Subheadline: `data-i18n="sre_page.hero.subheadline"`
- Price: `data-i18n="services.sre.price"` ($5,500) and `data-i18n="services.sre.price_period"` (/month) — reuse existing keys

**Problem section:**
- `data-i18n="sre_page.problem.body_1"` — only 1 paragraph (SRE problem statement is more concise)

**Deliverables:**
- Section heading: `data-i18n="sre_page.deliverables.heading"`
- Intro: `data-i18n="sre_page.deliverables.intro"`
- 6 deliverable items using `sre_page.deliverables.items.0` through `.5`
- Number badges use `deliverable__num--amber` class (SRE = amber/warning accent)
- Badge frequency pills use `deliverable__badge--freq-amber` class

**Outcomes:**
- 3 cards using `sre_page.outcomes.items.0` through `.2`

**Timeline:**
- Steps using `sre_page.how.steps.0` through `.4` (5 steps for SRE)

**CTA section:**
- IDENTICAL to observability.html — reuses `cta_section.*` keys

**Footer scripts:**
- IDENTICAL to observability.html — same VANTIX_BASE, i18n, partials, Calendly, GA4

The entire file structure mirrors observability.html. The only differences are:
1. `<title>` and `<meta description>` content
2. `page-hero--sre` instead of `page-hero--observability`
3. All `data-i18n` keys use `sre_page.*` instead of `obs_page.*`
4. Price keys use `services.sre.price` instead of `services.observability.price`
5. Number badges use `--amber` instead of `--green`
6. Different number of deliverable items and timeline steps (based on sre_page keys in en.json)
  </action>
  <verify>
    <automated>test -f 06-landing-pages/services/fractional-sre.html && grep -c "data-i18n" 06-landing-pages/services/fractional-sre.html | awk '{if ($1 >= 25) print "PASS: " $1 " i18n attributes"; else print "FAIL: only " $1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep 'data-partial="nav"' 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep 'data-partial="footer"' 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "VANTIX_BASE = '\\.\\.'" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "js-calendly-trigger" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "page-hero--sre" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "sre_page\\.hero\\.pill" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "sre_page\\.deliverables\\.items" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "sre_page\\.outcomes\\.items" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "sre_page\\.how\\.steps" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "cta_section\\.heading" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "cta_section\\.cta_primary" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "\\.\\./css/tokens\\.css" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "\\.\\./css/detail\\.css" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "\\.\\./js/i18n\\.js" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "\\.\\./js/partials\\.js" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "G-XXXXXXXXXX" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "calendly\\.event_scheduled" 06-landing-pages/services/fractional-sre.html` exits 0
    - `grep "services\\.sre\\.price" 06-landing-pages/services/fractional-sre.html` exits 0 (canonical SRE pricing key used)
    - `grep "deliverable__num--amber" 06-landing-pages/services/fractional-sre.html` exits 0 (SRE uses amber accent)
  </acceptance_criteria>
  <done>fractional-sre.html exists with page-hero (amber accent), deliverables, outcomes, timeline, and CTA section. All text uses data-i18n attributes with sre_page.* keys. VANTIX_BASE is '..' and all paths use ../ prefix. Calendly popup trigger and GA4 booking listener are present. Price shows $5,500/month via services.sre.price key.</done>
</task>

</tasks>

<verification>
1. Both HTML files exist in `06-landing-pages/services/`
2. Both files have `data-partial="nav"` and `data-partial="footer"` divs
3. Both files have `window.VANTIX_BASE = '..'` as the first script
4. Both files reference `../css/tokens.css`, `../css/base.css`, `../css/detail.css`
5. Both files have the Calendly popup trigger script with `js-calendly-trigger` selector
6. Both files have the GA4 booking event listener for `calendly.event_scheduled`
7. All text content uses `data-i18n` attributes — no hard-coded user-visible strings
8. observability.html uses `obs_page.*` keys and green accent classes
9. fractional-sre.html uses `sre_page.*` keys and amber accent classes
10. Both files use canonical pricing from existing `services.observability.price` and `services.sre.price` keys
</verification>

<success_criteria>
- Opening `http://localhost:8080/services/observability.html` shows dark-themed page with nav, footer, all sections, and working Calendly popup
- Opening `http://localhost:8080/services/fractional-sre.html` shows the same with SRE content and amber accents
- Toggling language switches all visible text on both pages
- Clicking "Book a Free Demo" on both pages opens the Calendly popup widget
</success_criteria>

<output>
After completion, create `.planning/phases/03-detail-pages/03-02-SUMMARY.md`
</output>
